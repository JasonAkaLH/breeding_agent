# gene_check JSON

用户可以提供既有 gene_check JSON。此时应把它作为 single source of truth 解读，而不是重新编造或外推。

解释 gene_check JSON 时优先提取：

- 样本列表。
- QTN records。
- trait 信息。
- favorable variant 统计。
- review notes / unmatched / missing / complex cases。

不要把完整 gene_check JSON 直接贴给用户；优先给结构化摘要和与用户问题相关的字段解释。
