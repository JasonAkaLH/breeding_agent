schema_id: diagonal
version: 1.0.0
title: Diagonal augmented design input
description: 对角线增广试验设计输入，要求提供材料清单和田块列数 ncols。
activation:
  aliases:
    - diagonal
    - 对角线
    - 对角线增广
    - diagonal augmented
inputs:
  material_data:
    type: artifact
    required: false
    title: 材料清单
    question: 请上传或提供材料清单文件，推荐列名为 ped_id、hyb_check、set。
    source:
      allowed:
        - artifact
        - upload
        - task_attachment
        - upload_ledger
        - metadata
    aliases:
      - material_data
      - material_file
      - input_file
      - 材料清单
      - 材料表
    validation:
      file_extensions: [.csv, .xls, .xlsx]
  input_data:
    type: string
    required: false
    title: 粘贴的材料表
    question: 如果没有文件，也可以直接粘贴 CSV 格式材料表。
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    aliases:
      - input_data
      - pasted_material_data
      - pasted_data
      - 材料表
  design:
    type: string
    required: true
    title: 设计类型
    question: 设计类型已选择为 Diagonal。
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    const: diagonal
    enum: [diagonal]
    aliases: [design, design_type, 设计类型, 试验设计]
    patterns:
      - '(?i)\b(diagonal|diagonal augmented)\b'
      - '对角线|增广'
  ncols:
    type: integer
    required: true
    title: 田块列数
    question: 请提供田块列数 ncols，例如 ncols=20 或列数20。
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    aliases: [ncols, columns, field_columns, 列数, 田块列数]
    patterns:
      - '(?:ncols|columns?|列数|田块列数)\s*[:：=]?\s*(\d+)'
      - '(\d+)\s*(?:列|columns?)'
    validation:
      min: 1
  ck_ratio:
    type: string
    required: false
    title: 对照比例等级
    question: 对照比例等级可选 A、B、C，默认 A。
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    default: A
    enum: [A, B, C]
    aliases: [ck_ratio, ck-ratio, check_ratio, 对照比例]
    patterns:
      - '(?:ck_ratio|ck-ratio|check_ratio|对照比例)\s*[:：=]?\s*([ABCabc])'
  site_num:
    type: integer
    required: false
    title: 试验地点数
    question: 如果需要多个试点/试验地点，请提供地点数；默认 1 个地点。
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    default: 1
    aliases: [site_num, site_count, sites, 试点数, 试点, 试验地点数, 地点数, 地点]
    patterns:
      - '(?:site_num|site_count|sites?|试点数|试验地点数|地点数|地点|试点)\s*[:：=]?\s*(\d+)'
      - '(\d+)\s*(?:个|处)?(?:试点|试验地点|地点|site|sites)'
      - '做\s*(\d+)\s*(?:次|套|个试点|个地点)'
      - '生成\s*(\d+)\s*套'
      - '(\d+)\s*套(?:不一样|不同|独立)?'
    validation:
      min: 1
      max: 50
  planter:
    type: string
    required: false
    title: 播种路径
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    default: serpentine
    enum: [serpentine, cartesian]
    aliases: [planter, planting_path, 播种路径, 走向]
    patterns:
      - '(?i)(serpentine|cartesian)'
      - '蛇形|往返'
      - '笛卡尔|逐行'
  randomize:
    type: boolean
    required: false
    title: 是否随机化
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    default: true
    aliases: [randomize, randomise, 随机化, 打乱]
    patterns:
      - '(?:randomi[sz]e|随机化|打乱)\s*[:：=]?\s*(true|false|yes|no|是|否)'
  seed:
    type: integer
    required: false
    title: 随机种子
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    aliases: [seed, random_seed, 随机种子]
    patterns:
      - '(?:seed|random_seed|随机种子)\s*[:：=]?\s*(\d+)'
    validation:
      min: 1
  run_id:
    type: string
    required: false
    title: 运行名称
    source:
      allowed:
        - query
        - current_user_message
        - resolved_user_message
        - recent_user_message
        - text
        - user_text
        - metadata
    aliases: [run_id, run-id, 运行名称, 任务名]
constraints:
  - any_of: [material_data, input_data]
