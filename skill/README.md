# Project Skills

本目录用于存放本项目后端可加载的项目级 Skill。

放置规则：

```text
skill/<skill-name>/SKILL.md
skill/<skill-name>/scripts/<optional-python-script>.py
```

约束与示例见：`Skill构建指南.md`。

说明：
- `SKILL.md` 是每个 Skill 的唯一必需文件。
- 后端默认扫描仓库根目录下的 `skill/**/SKILL.md`。
