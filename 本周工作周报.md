# 本周工作周报

> 周期：2026-05-25 至 2026-05-29
> 范围：`breeding_agent` 小奥 Agent 主项目
> 来源：本周当前分支提交历史与 `CHANGELOG.md` 近期记录

## 1. 本周主项目完成多条生产化链路推进，重点从“功能可用”继续推进到“可安全上线、可长期维护”

1. 认证与身份边界完成一次集中收口：
   1. 将受保护 API 的用户身份统一收敛到 Bearer token，不再从 Cookie、body 或旧 username 字段推断用户。
   2. 清理 legacy auth context，并同步核心 contract、Rust contract snapshot、前端 API client 与 API 文档。
   3. 相关测试覆盖登录隔离、token 迁移、上传 / 会话 / 任务权限和历史记忆归属。
2. PostgreSQL 状态平台进入生产化基础阶段：
   1. 新增 PostgreSQL state platform 的 command、write queue、read store、health、migration、runtime factory 与验证脚本。
   2. 会话、任务、SSE、强删除、SQLite 清理和 PostgreSQL cutover 语义完成一轮联动改造。
   3. 补齐远端部署诊断、PostgreSQL 网络接入和 cutover runbook，方便后续真实环境切换。
3. 繁育业务 Skill 与 Slash Command 体验继续完善：
   1. Field Design、Field Analysis、Rice Genie、OCR、SQL Query 等项目级 Skill 的展示名、中文说明、输入契约和前端选择体验完成统一。
   2. Slash Skill 从“直接硬执行”调整为“软绑定”：先由主代理判断是答疑还是执行，再进入确定性 Skill 编排。
   3. 缺参 interrupt、历史追问、流式答疑、Skill 节点恢复和最终回答文件下载边界完成补齐。
4. 大语言模型 Prompt Envelope 架构完成分阶段落地：
   1. 完成 Prompt Envelope 总体 PRD 与 P0-P7 阶段拆分，并落地核心 renderer、主代理 string mode、对话记忆候选、工具信息安全档案、多调用场景 profile、messages-native 扩展和 provider cache 观测。
   2. 将 prompt token 预算统一到选中模型配置，增加 75% 输入预算、历史裁剪、fail-closed preflight、no-raw audit 与稳定 prefix 污染防护。
   3. 将 Skill public profile、tool schema、conversation memory、planner / replanner / input resolver 等 LLM 调用链路逐步纳入同一套 prompt profile 管理。

## 2. 本周主要修复和加固的 bug 大类

1. 身份与权限类问题：修复旧身份来源残留、用户可伪造身份字段、API 文档与 token-only 鉴权口径不一致等问题。
2. 状态与恢复类问题：修复 PostgreSQL cutover 后会话强删除不彻底、SSE auth generation 失效不及时、长 reasoning / thinking 事件影响最终回答恢复、interrupted Skill 回答后重复创建节点等问题。
3. Skill 执行体验类问题：修复 Skill 缺参只返回失败而不是结构化 interrupt、`/skill` 追问缺少历史上下文、软绑定答疑不流式、SQLQuery 行数超限直接硬失败、前端 terminal failure 气泡卡住等问题。
4. 文件与 artifact 安全类问题：修复最终回答可能编造 `sandbox:`、本地路径或非平台下载链接的问题，统一要求只有平台 artifact download URL 才能声明“可下载”。
5. Docker / 部署类问题：修复 Field Analysis 在 Docker 内缺少 R `jsonlite` 和 UTF-8 locale 导致中文 R 源码解析失败的问题，并持续同步远端镜像 tag 与启动命令。
6. Prompt / 上下文安全类问题：修复 prompt 中暴露 Skill 内部 body、脚本路径、handler、runtime 配置或 raw artifact 的风险，并补齐 provider role fallback、cache hint 和 audit 安全边界。

## 3. 本周文档、部署与工程治理交付物

1. 文档层面：更新 REST API 静态文档、Docker 远端部署命令、PostgreSQL state platform runbook、Prompt Envelope 系列 PRD、表格上传编码兼容与表头规范化 PRD。
2. 部署层面：推进 backend / frontend Docker 镜像版本与远端启动文档同步，记录 PostgreSQL 网络、容器别名、镜像检查和日志排障步骤。
3. 工程治理层面：将 Skill runtime 兼容层迁移到更中性的 `agent_skills` 命名空间，抽出通用 coercion、evidence、LLM stream event、provider metadata 等 helper，减少运行时重复代码。
4. 质量门禁层面：本周提交多次覆盖后端 unittest、前端 Vitest / build、compileall、git diff check、cargo fmt / test / check / deny、PyO3 wheel smoke、Docker build / push 等验证；真实生产 shadow、远端 provider smoke 和部分 deployment gate 仍保持后续验证项。

## 4. 下周建议重点

1. 优先完成 PostgreSQL 真实环境 cutover、备份 / 恢复 / 回滚演练和生产 shadow 证据，避免只停留在 repo-local 验证。
2. 继续推进 Prompt Envelope 的灰度与观测，重点看真实 provider 下的 token 预算、messages mode、cache hint 和最终回答质量。
3. 对 Field Design / Field Analysis / Rice Genie / OCR 等项目级 Skill 做一轮真实文件上传与端到端浏览器联调，验证软绑定、缺参续答、artifact 下载和 Docker 运行时一致性。
4. 表格上传编码兼容与表头规范化 PRD 已完成边界定义，后续可按分阶段方案进入实现与测试。

> 本周整体进展：`breeding_agent` 主项目围绕认证安全、PostgreSQL 状态平台、项目级 Skill 体验、Prompt Envelope 架构、Docker 部署和工程去重做了一轮集中推进；同时修复了一批身份边界、状态恢复、Skill 续答、文件下载、Docker 运行时和 prompt 安全相关的问题，为后续真实生产部署和端到端联调打下基础。
