# 本周工作周报

> 周期：2026-05-18 至 2026-05-22
> 范围：`breeding_agent` 小奥 Agent 主项目、`ocr_mcp` OCR MCP 服务项目

## 1. 小奥 Agent 主项目完成多条产品化链路收口，重点从“能力可用”推进到“可部署、可恢复、可对接”

1. 多 Skill 编排链路继续完善：
   1. 修复 public Skill 之间被 LLM 规划依赖误阻塞的问题，使多个独立 Skill 可以并行执行。
   2. 将多 Skill 回答收敛为单一全局 finalizer，避免每个 Skill 都产生中间 assistant 最终回答。
   3. 新增轻量 `response_role=intermediate|final` 契约，保证会话历史和 conversation memory 优先保留最终回答。
   4. 将 Skill 执行中的临时进度改为 assistant 气泡外的轻量状态行，不污染聊天正文、历史消息和长期记忆。
2. Slash Skill Command 能力完成前后端闭环：
   1. 前端登录后拉取 public `skill.*` capability，支持 `/` picker、键盘/鼠标选择、Skill badge、直接 `/skill args` 提交。
   2. 后端支持 `force_capability` 显式路由，并在 capability 不存在时 fail-closed。
   3. 新增 pending Skill context：当显式 Skill 缺少 typed input 时，会保存待续接上下文；下一轮普通消息优先续接原 Skill。
   4. 新 slash 可以覆盖旧 pending，用户 metadata 不能伪造续接，避免 Skill 状态被前端或用户输入绕过。
3. 历史会话与当前任务恢复能力增强：
   1. 明确 current task 刷新恢复 PRD 和测试规格，聚焦同一 conversation 下 `current_task_id` 的运行态恢复。
   2. 前端支持刷新/切换历史会话后恢复 running、waiting input、terminal/stale 等任务状态。
   3. 增加 generation guard 和正式 history 去重策略，减少异步恢复过程中的旧请求覆盖新会话问题。
4. 安全认证与多客户端边界完成升级：
   1. 登录/注册改为 `__Host-maf_session` opaque Secure Cookie，并保留旧 `maf_session` 只读迁移。
   2. 新增 scoped API token 创建、列表、撤销与 Bearer 业务 API 鉴权。
   3. 补齐 CORS allowlist、前端 Bearer REST / SSE client、token revoke / last-used 并发安全边界。
   4. 明确 Cookie 适合同站同域浏览器，Bearer token 面向跨站/第三方浏览器/非浏览器客户端。
5. REST API 文档进入正式交付形态：
   1. 将静态 REST API 文档迁移到 `docs/api/api-doc.html`，后端新增只读入口 `GET /api-doc`。
   2. 补齐 26 个 API operation 的请求参数、响应字段、错误码、认证 Cookie、Bearer token、SSE 调用注意事项。
   3. 调整官方非 GET 路由，不再把业务 ID 放在 URL path，统一放到 JSON body 或 multipart form 中。
6. OCR Skill 接入链路增强：
   1. 上传链路支持 PNG/JPG/JPEG/PDF，二进制文件通过脚本专用 `content_base64` artifact 传给 Skill。
   2. OCR Skill 成功时同时输出给主代理总结的识别内容和前端可展示的 `ocr_raw_text` artifact。
   3. OCR 远端服务地址、token、超时和轮询参数改为读取 git-ignored `skill/ocr/config.yaml`，与根目录 `config.yaml` 隔离。
   4. Skill 失败输出补充 `error_code`、`error_type`、`stage`、`retriable` 和 audit-only 脱敏事件，便于排查远端 OCR MCP 问题。
7. Docker 部署基线完成：
   1. 新增多阶段 `Dockerfile`、`.dockerignore`、`docker-compose.yml` 和 nginx 反代配置。
   2. backend 镜像基于 Ubuntu 22.04 + Conda Python 3.13.13，frontend 镜像基于 Ubuntu 22.04 + nginx。
   3. Docker context 排除 `tests/`、根目录 Markdown 文档、除 `docs/api` 外的 docs，同时按部署要求把本地 `config.yaml` 打入 backend 镜像。
   4. 已构建并推送 `0.1.0` 镜像到阿里云镜像仓库：
      1. `registry.cn-hangzhou.aliyuncs.com/biobin/breeding-agent-backend:0.1.0`
      2. `registry.cn-hangzhou.aliyuncs.com/biobin/breeding-agent-frontend:0.1.0`
   5. 根目录新增 `docker_cmd.md`，记录远端 pull、network/volume、`docker run`、日志查看和停止命令；宿主机端口调整为 backend `51888`、frontend `51999`。
   6. 明确 `/app/runtime` 挂载到 Docker named volume `breeding-agent-runtime`，升级镜像/重建容器不删除 volume 即可保留 SQLite、audit 和 artifact 数据。

## 2. MCP Client / Rust Runtime 相关工作继续推进，重点是兼容性、证据门禁和 license gate

1. MCP Client 四版本兼容进入分阶段实现与证据收口：
   1. 建立 `2024-11-05`、`2025-03-26`、`2025-06-18`、`2025-11-25` 四版本兼容矩阵。
   2. 完成协议协商内核、2024 legacy HTTP+SSE transport、2025+ Streamable HTTP fixture 和 conformance gate。
   3. 2024 legacy HTTP+SSE 支持 persistent SSE reader、endpoint event、POST endpoint 校验、notification 204、response id correlation 和 cleanup。
   4. 2025+ Streamable HTTP 覆盖 protocol header、session、initialize/initialized、tools/list、tools/call、GET/DELETE 405、session 404 等兼容场景。
2. 官方 Rust SDK 接入按 adapter / shadow compare / enforce gate 路径推进：
   1. `rmcp 1.7.0` 作为 client-only Streamable HTTP 依赖进入 Rust workspace。
   2. Rust adapter metadata、compile-time marker、SDK-backed initialize/list/call/close/diagnostics、错误映射与 lockfile drift guard 已落地。
   3. 明确 `2024-11-05 + legacy_http_sse` official SDK lane 是 unsupported transport，由 Python legacy visible path 继续覆盖。
3. Rust PRD evidence gate 继续保持 fail-closed：
   1. PRD03-PRD07 evidence validator 的重复逻辑收敛到共享 helper，减少 gate drift。
   2. PRD07 orchestration hotspot 维持 guarded，不在缺少独立实施 PRD、性能/可靠性证据前创建新 Rust kernel。
   3. PRD05 MCP Runtime evidence ledger 补充 repo-local client compatibility conformance 证据，但 production shadow、benchmark、ops/recovery、legacy decommission 仍保持 pending gate。
4. License gate 保持严格：
   1. 因 `rmcp -> reqwest -> rustls-platform-verifier -> webpki-root-certs` 引入 `CDLA-Permissive-2.0`，在 `native/deny.toml` 中对 `webpki-root-certs = 1.0.7` 添加精确 exception。
   2. 没有全局放宽 license allowlist，继续保持 `cargo deny check` fail-closed 语义。

## 3. `ocr_mcp` 项目完成交付级 OCR MCP 服务骨架与生产化约束

1. 明确 OCR MCP 服务契约：
   1. 服务面向 MCP `2025-11-25` Streamable HTTP，endpoint 为 `POST /mcp`。
   2. 生产 tools 固定为 `get_ocr_capabilities`、`start_parse_job`、`get_parse_job`、`ack_parse_job`、`cancel_parse_job`。
   3. OCR 执行只走异步 job，不暴露生产同步 `parse_document` tool。
   4. 支持 `shared_volume`、HTTPS URL、`upload_id`、小文件 `base64` fallback；`file_uri` 默认禁用。
   5. 不提供长期 artifact 下载或 `/artifacts/*`，结果通过 `get_parse_job` 返回 JSON/Markdown，调用方保存后必须 `ack_parse_job`。
2. Docker / CUDA / GPU 部署基线完成：
   1. Dockerfile 使用 `nvidia/cuda:12.6.0-cudnn-devel-ubuntu22.04` builder 和 `nvidia/cuda:12.6.0-cudnn-runtime-ubuntu22.04` runtime。
   2. 安装 PaddlePaddle cu126 GPU wheel、`paddleocr[doc-parser]`、PyMuPDF 等 OCR 运行依赖。
   3. 模型目录 `models/PaddleOCR-VL-1.5` 和 `models/PP-DocLayoutV3` 打包进镜像，不依赖运行时挂载模型。
   4. runtime 使用非 root 用户 `ocr`，默认 `OCR_MCP_DEVICE=gpu:1`，暴露容器内 `8080`。
   5. Compose 固定 `platform: linux/amd64`，通过 Docker GPU reservation 使用单张 GPU，默认加入外部 `ocr_net` 且不暴露宿主机端口。
   6. 已形成远端启动命令文档，当前镜像示例为 `registry.cn-hangzhou.aliyuncs.com/biobin/ocr_mcp:0.12.4`，端口调试命令映射到宿主机 `51789`。
3. 配置、限流和安全边界补齐：
   1. `.env.example` 固化默认限制：100MB 文件、10MB base64、PDF 200 页、DPI 200、job/upload TTL 24h、inline 16MB、chunk 4MB、并发 1、队列 100、超时 3600s。
   2. 支持 Bearer token、Origin allowlist、HTTPS URL allowlist、私网地址拒绝和 redirect 前校验，降低 SSRF 风险。
   3. shared volume 输入拒绝绝对路径、`..`、隐藏路径、软链接、hardlink、非普通文件和未完成 `.tmp` 文件。
   4. 上传输入做扩展名、MIME、魔数一致性校验，保留二进制尾部 CRLF，不破坏原始文件 hash。
   5. `/metrics` 输出 Prometheus 文本指标，不记录 token、URL query 或正文。
4. Job lifecycle 与可观测性增强：
   1. JobManager 支持 queued/running/succeeded/failed/cancelled/acknowledged 等状态。
   2. 服务重启时将 running snapshot 标记为 failed，并返回 `SERVICE_RESTARTED`。
   3. running job 被取消后，迟到的 backend result 不会覆盖 cancelled 状态。
   4. 生命周期日志覆盖 `ocr_job_enqueued`、`ocr_job_running`、`ocr_job_backend_parse_start`、`ocr_job_succeeded` 等关键阶段。
   5. running poll 和 tool error 日志脱敏，不泄露 Bearer token。
5. PaddleOCR-VL 后端集成增强：
   1. PaddleOCR-VL backend 支持 lazy load 和 startup preload，`/readyz` 可反映模型加载状态。
   2. 加载时使用本地模型目录和本地 cache，设置 `PADDLE_PDX_MODEL_SOURCE=local`，避免运行时走远端下载。
   3. PDF 先按页渲染为图片，再进入 PaddleOCR-VL 推理；支持页码选择和最大页数限制。
   4. raw output 通过 `save_to_json` 等价路径提取，并映射 table、formula、seal、text 等 block 到结构化结果和 Markdown。
6. OCR Skill 桥接脚本形成可对接链路：
   1. `skill/ocr/scripts/run_ocr.py` 实现上传文件、MCP initialize、`start_parse_job`、轮询 `get_parse_job`、最终 `ack_parse_job` 的完整序列。
   2. 脚本通过 stderr 输出 `OCR_SKILL_PROGRESS` 进度事件，不在进度日志中泄露 Authorization token。
   3. 成功输出 Markdown answer、job_id 和结构化 OCR 内容，为小奥 Agent OCR Skill 接入提供外部服务基础。

## 4. 本周验证与交付物

1. 小奥 Agent 主项目验证覆盖：
   1. 后端 storage / API / orchestration / lifecycle / capability / e2e 等分层 unittest。
   2. 前端 Vitest、typecheck、build。
   3. Docker `docker compose config`、backend/frontend 镜像 build、容器内容检查、compose up smoke。
   4. 远端镜像 tag `0.1.0` 已 push 到阿里云仓库。
2. OCR MCP 项目验证覆盖：
   1. pytest 覆盖配置默认值、环境变量覆盖、PDF 页码解析、部署 artifact 存在性。
   2. pytest 覆盖 PaddleOCR-VL backend lazy/preload、本地模型目录、GPU device 参数、PDF render、raw JSON 映射、生命周期日志。
   3. pytest 覆盖 rate limit、active job limit、重启恢复、source 安全校验、redirect SSRF 防护、取消迟到结果、上传二进制完整性。
   4. pytest 覆盖 Dockerfile / Compose / caller integration 文档约束，以及 OCR Skill 脚本 MCP 调用序列和 token 脱敏。
3. 文档与部署交付物：
   1. `breeding_agent`：`Dockerfile`、`docker-compose.yml`、`docker/nginx.conf`、`.dockerignore`、`docker_cmd.md`、`README.md`、`AGENTS.md`、`CHANGELOG.md`。
   2. `ocr_mcp`：`Dockerfile`、`docker-compose.yml`、`.env.example`、`DEBUG_STARTUP.md`、`TEST_COMMAND.txt`、`README.md`、`docs/CALLER_DOCKER_INTEGRATION.md`。

## 5. 下周建议重点

1. 小奥 Agent 远端部署需要尽快补齐 HTTPS 域名入口：当前浏览器登录 Cookie 使用 `__Host-maf_session; Secure`，如果通过 `http://远端IP:51999` 访问，登录成功后后续 `/api/v1/conversations` 仍会 401；生产建议走 HTTPS 域名，开发临时 HTTP 方案需要另做明确开关。
2. Docker 镜像当前按要求打入 `config.yaml`，后续若进入多人/生产发布，建议改为 runtime secret、volume mount 或部署环境变量，避免敏感配置固化进镜像层。
3. 小奥 Agent 与 OCR MCP 的正式联调需要在目标 Linux + CUDA 12.6 + V100 环境验证，尤其是 OCR MCP GPU 推理、超大 PDF、长任务轮询、ack 清理和小奥 Agent OCR Skill 端到端展示。
4. MCP Client / Rust Runtime 仍需继续补齐 production shadow、benchmark、ops/recovery drill、deployment allowlist promotion 与 legacy decommission 证据，不能用本地合成证据替代生产门禁。
5. 继续把部署命令、端口、volume、日志查看、故障排查文档化，降低远端环境排障成本。

> 本周整体进展：小奥 Agent 主项目完成 Skill、认证、历史恢复、API 文档、OCR Skill 与 Docker 发布链路的集中收口；OCR MCP 项目完成交付级异步 OCR MCP 服务的 Docker/GPU、安全、Job lifecycle 和 Skill 桥接基础，为后续两项目在真实服务器上的端到端联调打下基础。
