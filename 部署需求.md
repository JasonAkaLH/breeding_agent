# breeding_agent 部署需求（可扩展版）

## 一、部署目标与规模

本方案以 **1 台应用服务器 + 1 台数据库/平台服务服务器** 为起点，面向中小型团队的生产落地。  
重点目标是：

- 可用性：尽量减少单点故障对外部服务影响  
- 可发布性：版本切换可回滚，发布窗口可控  
- 可扩展性：支持在不改架构的前提下增加应用副本  
- 可观测性：告警、日志和追踪闭环可用

## 二、硬件需求清单（推荐，不是最低配置）

| 服务器角色 | CPU | 内存 | 磁盘 | 网络 | 推荐规格（每台） | 用途与说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 应用服务器 | 12~16 vCPU（64-bit x86） | 48 GB ECC DDR4/DDR5 | 系统盘 300 GB NVMe + 数据盘 500 GB NVMe | 1Gbps（内网建议 10Gbps） | 首发版 | 运行 `breeding_agent` 后端实例（建议 2~4 个 worker） |
| 数据与平台服务服务器（DB/共享） | 16 vCPU（64-bit x86） | 64 GB ECC DDR4/DDR5 | 数据盘 1~2 TB NVMe（数据库）+ 1 TB NVMe（WAL/日志） | 1Gbps（内网建议 10Gbps） | 首发版 | 运行 PostgreSQL（主库）与平台运行时文件；可先同机部署 Redis/缓存 |

### 配套参数（所有服务器）

- 操作系统：Ubuntu Server 22.04 及以上，64-bit  
- 内存优先 ECC  
- 磁盘建议以 NVMe 为主。I/O 目标按压测结果与真实写放大比例调整  
- 网络建议内网口 ≥ 10Gbps  
- 电源与机柜建议配备 UPS，支持关键服务降级策略

## 三、部署边界与运维要求

- **需要运维：2 人**
  - 人员 A：智能体部署和应用运维（发布、回滚、配置、状态治理、任务链路排障）
  - 人员 B：服务器部署和运维（系统、网络、补丁、安全、数据库与存储底座）
- 运维职责：
  - 发布与回滚（镜像版本、配置变更、数据库变更窗口）  
  - 监控告警（CPU、内存、连接数、磁盘 I/O、SSE 长连接数、数据库延迟）  
  - 备份与恢复演练（数据库、runtime 文件、日志）  
  - 证书与密钥轮换、账号/权限变更审计  
  - 性能容量评估（每周）与告警阈值维护（容量告警阈值提前预警）

## 四、扩容与升级路径（建议）

1. **应用层扩容**：当 CPU 持续高位（如长期 > 65%）或 SSE 长连接压力持续上升时，先从 1 台扩到 2 台应用实例  
2. **数据库后续扩**：当 I/O 等待时间升高或 WAL 相关指标持续告警，优先优化磁盘与参数，再考虑分离日志盘与只读副本  
3. **中间件解耦**：任务并发上升且现有组件接近瓶颈时，分离缓存与队列服务，梳理职责边界
