name: Repository policy

on:
  pull_request:
  push:
    branches: [main, prod]
  workflow_dispatch:

jobs:
  docker-cmd-local-only:
    name: Deployment commands remain local-only
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Verify deployment command policy
        run: bash scripts/check_docker_cmd_policy.sh
